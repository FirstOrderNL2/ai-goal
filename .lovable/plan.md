

# Horizontal Scrollable League Filter

## Change
**File: `src/components/LeagueFilter.tsx`**
- Replace `flex-wrap` with a horizontal scroll container using `overflow-x-auto` and `flex-nowrap`
- Add `scrollbar-hide` styling for a clean look (hide scrollbar but keep scroll functionality)
- Ensure buttons don't shrink with `shrink-0`

Single file change, minimal diff.

